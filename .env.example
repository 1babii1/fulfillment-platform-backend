# Local development-only Compose contract. Replace the placeholder in your untracked .env file.
POSTGRES_PASSWORD=replace-for-local-development
# Optional: OTLP endpoint, e.g. http://localhost:4317. Leave unset when no collector is running.
# OTEL_EXPORTER_OTLP_ENDPOINT=
