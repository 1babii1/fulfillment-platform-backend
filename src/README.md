# Source modules

Application source will be extracted incrementally. Every module must build with only portfolio-safe configuration and must not depend on production-only providers or infrastructure values.
