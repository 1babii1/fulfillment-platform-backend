# Infrastructure examples

This directory will contain local Docker assets plus generic Kubernetes and Ansible examples. It must never contain production inventories, credentials, infrastructure addresses, kubeconfig files or deployment-specific topology.
