# Infrastructure examples

This directory contains local Docker assets plus generic Kubernetes and Ansible examples. It must never contain production inventories, credentials, infrastructure addresses, kubeconfig files or deployment-specific topology.

See [docker/README.md](docker/README.md) for the local Compose environment.

See [kubernetes/README.md](kubernetes/README.md) for static manifest validation and the generic rollout/rollback procedure.
